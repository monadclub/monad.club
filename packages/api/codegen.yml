overwrite: true
schema:
  - ./lib/schema/*/type-defs.graphql
documents: null
generates:
  ./lib/generated/graphql.ts:
    plugins:
      - typescript-common
      - typescript-server
      - typescript-resolvers
