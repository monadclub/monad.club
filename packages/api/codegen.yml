overwrite: true
schema: http://localhost:4000/
documents: null
generates:
  ./lib/generated/graphql.d.ts:
    plugins:
      - typescript-common
      - typescript-server
      - typescript-resolvers
